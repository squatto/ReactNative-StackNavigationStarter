# React Native Stack Navigation Starter

## Includes

* Simple stack navigation
* Redux
* Two views in a stack

## Structure

* Copy `src` to the root of your project
* Modify `./index.js` to `import App from './src/App';`

